{
  "name": "pawprint",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npx concurrently -n backend,frontend -c blue,green \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "node scripts/run-backend.js",
    "dev:app": "npm run dev --prefix cloud.adapdog/www",
    "install:backend": "node scripts/setup-backend.js",
    "build": "npm run build --prefix frontend",
    "install:all": "npm install && npm install --prefix frontend && npm install --prefix cloud.adapdog/www && npm run install:backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
